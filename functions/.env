# Non-secret function params (secrets like SEMAPHORE_API_KEY live in Secret Manager).
# POS bridge base URL — set to the deployed Phase-1 posApi URL when wiring the POS bridge.
POS_API_BASE=
# Optional Semaphore registered sender name (blank = Semaphore default sender).
SEMAPHORE_SENDER_NAME=
